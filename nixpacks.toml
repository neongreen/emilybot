# Used by Coolify

[phases.setup]
nixPkgs = [
  "...",
  # Install sops and age so that we can decrypt the secrets from .env.yaml
  "sops",
  "age",
  # For the JS execution
  "deno",
]

[start]
# Run the app with the decrypted secrets
cmd = "sops exec-env .env.yaml 'uv run emilybot'"
