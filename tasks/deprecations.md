# start collecting stats about called commands

# deprecate .cmd (start showing a warning)

# ✅ list other things to deprecate

# deprecate .random

# deprecate `$.lib` and replace with idk just `lib`?

# deprecate `$.message`, `$.user`, `$.ctx`
